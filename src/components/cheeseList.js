import React from 'react'

export default function CheeseList(props){
  // const cheeseArray = props.cheeseData.map((cheeseName,index) => <li key={index}>{cheeseName}</li>)
  console.log('testcheese arr',props.cheeseData);
  return(
    <ul><li>test</li></ul>
  )
}
